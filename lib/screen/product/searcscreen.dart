import 'package:flutter/material.dart';

class SearhProduct extends StatelessWidget {
  const SearhProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Search Window");
  }
}
